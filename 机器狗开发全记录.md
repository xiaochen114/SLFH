# 绝影Lite3 机器狗开发全记录

> 时间跨度：2026-06-04 ~ 2026-06-17
> 项目：异构多机器人中央调度系统（森林防火巡逻）
> 机器狗：云深处绝影Lite3 · 雷达：RoboSense C16 · 感知主机：192.168.1.103 · 运动主机：192.168.1.120

---

## 一、项目概况

中央大脑 + 异构机器人统一接口架构，以森林防火为落地场景。

### 核心文件

| 文件 | 用途 |
|------|------|
| `E:\zy\主控.py` | 巡逻主入口（YOLO感知+狗控制+Web面板） |
| `E:\zy\中央大脑\main.py` | 中央大脑入口 |
| `E:\zy\中央大脑\智能体.py` | LLM智能体（含技能引擎） |
| `E:\zy\中央大脑\自主巡逻.py` | Nav2巡逻调度器 |
| `E:\zy\机器人\机器狗_绝影.py` | 绝影Lite3 RobotBase适配 |
| `E:\zy\机器人\感知主机控制.py` | SSH操控感知主机ROS2 |
| `E:\zy\机器人\机器人模拟器.py` | 模拟机器人测试 |
| `E:\zy\web_dashboard.html` | 全面板（含地图/建图/巡逻/智能体） |
| `E:\zy\火情检测.py` | YOLO检测（火焰/烟雾/烟头） |
| `E:\zy\机器狗控制.py` | 绝影Lite3 UDP控制封装 |
| `E:\zy\配置.yaml` | 唯一配置源 |
| `E:\zy\巡逻点.yaml` | 巡逻点配置 |
| `E:\zy\中央大脑\技能引擎.py` | SKILL.md加载/匹配引擎 |
| `E:\zy\中央大脑\智能体_工具.py` | LLM工具集 |
| `E:\zy\中央大脑\智能体_钩子.py` | 生命周期钩子系统 |
| `E:\zy\中央大脑\智能体_技能锻造.py` | 自动生成技能 |

### 三本开发手册

```
E:\zy\
├── 绝影Lite3运动开发手册(beta) V2.2.0.pdf       ← 运动控制/UDP协议
├── 绝影Lite3运动主机通讯接口 V1.0.8.pdf          ← 指令码/通讯协议
└── 绝影Lite3感知开发手册(beta) U20 V2.2.3.pdf   ← ROS2/建图/导航
```

### 运行方式

```bash
cd E:\zy
python 中央大脑/main.py                 # 真机模式（注册绝影狗）
python 中央大脑/main.py --simulate      # 模拟模式
python 中央大脑/main.py --patrol        # 真机+巡逻
```

### WiFi配置

笔记本插网线连机器狗（有线LAN口设静态IP `192.168.1.100`，**网关留空**），WiFi正常上网。

### 智能体API

```
POST /api/agent/message  {message}  对话
GET  /api/agent/model               当前模型+目录
POST /api/agent/model  {model}      切换模型
POST /api/agent/mode   {active}     主动/被动模式
GET  /api/agent/skills              技能列表
POST /api/agent/skills/reload       重载技能
```

### 建图API

```
POST /api/lidar/start     {type:robosense}  开雷达
POST /api/lidar/stop                         关雷达
POST /api/mapping/start                      开始建图
POST /api/mapping/save    {name}             保存地图
POST /api/mapping/delete  {name}             删单张地图
POST /api/mapping/clear                      删全部地图
GET  /api/mapping/status                     地图列表+环境诊断
GET  /api/env/check                          环境诊断
POST /api/rosbridge/start                    启动rosbridge
```

---

## 二、建图故障排查（2026-06-13）

### 核心问题

faster-lio建图程序正常启动，Rviz无点云显示。激光雷达数据（`/rslidar_points`）有数据，但IMU数据（`/imu/data`）、里程计（`/leg_odom`）、关节状态（`/joint_states`）等所有来自运动主机的ROS话题均为零数据。

### 排查记录

```
1. 首次建图（ROS2 Humble）
   → faster-lio启动正常，日志 "lidar_type 4, Using C16 Lidar"
   → /cloud_registered 话题存在但无数据流（ros2 topic hz 卡住）
   → /rslidar_points 正常（1 publisher, 2 subscribers）

2. 检查IMU
   → /imu/data 话题存在（1 publisher）但无数据
   → 所有传感器话题（leg_odom/joint_states等）均零数据
   → c16.yaml配置正确：lid_topic="/rslidar_points", imu_topic="/imu/data"

3. 系统时间
   → 首次发现系统时间为2024年（差2年），修正后重建图
   → 问题依旧

4. UDP通信
   → ping 192.168.1.120 通
   → tcpdump port 43893/43894：零数据包
   → journalctl -u transfer_ros2：显示 "no [imu] data received since setup"

5. ROS2版本兼容性
   → transfer_ros2 跑在 Foxy(source /opt/ros/foxy/setup.bash)
   → faster-lio 跑在 Humble
   → 统一rmw_cyclonedds_cpp，问题依旧

6. 切换到ROS1 Noetic
   → sudo systemctl start transfer（ROS1版）
   → rostopic list 显示所有话题正常注册
   → 但 rostopic hz /imu/data 显示 "no new messages"
   → ROS1和ROS2表现完全一致 → 排除DDS问题

7. 重启机器狗本体
   → 重新起立 + 启动transfer
   → 问题依旧

8. 检查运动主机jy_exe配置
   → SSH到192.168.1.120（密码同感知主机）
   → cd ~/jy_exe/conf/
   → network.toml IP配置正常
```

### 根因定位

**运动主机（192.168.1.120）上的UDP数据发送程序未正常工作**。运动主机负责采集IMU/里程计/关节状态等数据，通过UDP 43893/43894端口发送给感知主机。当前该发送通道未开启，导致faster-lio因收不到IMU数据无法初始化，不发布点云。

### 待确认（需云深处技术支持）

1. 运动主机上UDP数据发送服务如何启动？是否有独立的systemd服务？
2. APP是否需要开启特定开关（传感器数据发布/ROS2通信）？
3. 运动主机上是否有感知功能总开关？
4. 机器狗出厂transfer程序是否需手动配置网络？

---

## 三、Web面板（2026-06-13 重构）

5个Tab页：

| Tab | 功能 |
|-----|------|
| 🤖 机器人 | 机器人卡片+控制按钮（起立/巡逻/停止/急停）+建图跳转 |
| 🚁 巡逻 | Canvas地图（rosbridge实时/手动加载）+点击选巡逻点 |
| 🗺️ 建图 | 雷达开关+建图启停+保存/删除地图 |
| 🧠 智能体 | 对话框（逐字动画）+API密钥弹窗+模型切换+主动/被动模式 |
| ⚙️ 环境 | ROS2/地图/Nav脚本/雷达ping诊断 |

---

## 四、智能体与技能系统（2026-06-13 重构）

### 6个技能

| 技能 | 触发词 | 说明 |
|------|--------|------|
| 头脑风暴 | 设计/需求/规划/架构 | 出设计文档，批准后再实施 |
| 写计划 | 计划/任务拆解 | 拆成2-5分钟小任务 |
| 子智能体驱动 | 执行/实现/编码/开发 | 派子智能体并行执行 |
| 代码审查 | 审查/review/检查 | P0/P1/P2三级问题报告 |
| 完成前验证 | 验证/测试/确认 | 运行验证命令，展示结果 |
| 技能锻造 | 技能/自动生成/工作流 | 检测重复模式→自动生成skill |

### 智能体工作流

用户消息 → PDF匹配(绕过LLM) → 技能匹配 → 加载SKILL.md → LLM执行 → 回复

### LLM模型切换器

13个厂商30+模型（DeepSeek V4/MiMo/Kimi/MiniMax/智谱GLM/通义千问/豆包/百川/阶跃星辰/零一万物/OpenAI/Ollama），支持Web面板点选、对话自然切换、LLM工具调用三种切法。

---

## 五、数据持久化

| 文件 | 用途 |
|------|------|
| `E:\zy\data\chat_history.json` | 聊天记录（最近500条） |
| `E:\zy\data\api_keys.json` | API密钥持久化 |

---

## 六、Hermes桥接（2026-06-16）

独立进程 `hermes_桥接.py`，不碰项目代码，每3秒检查 `ai_comm/to_claude/`。

```
E:\zy\ai_comm\
├── to_claude/   ← Hermes/CLI写，桥接读
├── to_hermes/   ← 桥接写，Hermes/CLI读
└── completed/   ← 已处理归档
```

消息格式：
```json
{"id":"x", "from":"hermes", "to":"claude", "type":"chat|task|status", "content":"消息"}
```

---

## 七、建图问题突破（2026-06-17）

### 推翻之前的结论

之前诊断"运动主机UDP不通导致IMU零数据"是**错误的**。实际根因：
1. **雷达驱动未启动** — `/rslidar_points` 无数据
2. **faster-lio 未运行** — `/cloud_registered` 不存在
3. **transfer_ros2 服务正常** — IMU 200Hz 稳定输出

### 网络配置确认（运动主机）

SSH到 192.168.1.120 查看 `/home/ysc/jy_exe/conf/network.toml`：
```toml
ip = '192.168.1.103'      # 目标=感知主机 ✅
target_port = 43897         # 上报端口
local_port = 43893          # 接收指令端口
```

### 排查过程

```
1. 确认 transfer_ros2 服务运行中 (active)
   → sudo systemctl status transfer_ros2 ✅

2. 端口监听确认
   → ss -ulnp | grep 43897 → 0.0.0.0:43897 监听中 ✅
   → ss -ulnp | grep 43899 → 0.0.0.0:43899 监听中 ✅

3. IMU 数据验证
   → ros2 topic hz /imu/data → ~200Hz ✅

4. 雷达数据验证
   → 需手动启动: cd ~/lite_cog_ros2/system/scripts/lidar && ./start_rslidar.sh
   → ros2 topic hz /rslidar_points → ~10Hz ✅

5. faster-lio 启动
   → 需创建 Log 目录: mkdir -p ~/lite_cog_ros2/slam/src/faster-lio/Log
   → cd ~/lite_cog_ros2/system/scripts/slam && ./start_slam.sh

6. 时间戳同步验证（Python脚本）
   → IMU 和 LiDAR 时间戳格式一致（纳秒），差值 81ms，可接受
```

### 雷达配置确认

感知主机 `/home/ysc/lite_cog_ros2/driver/rslidar_sdk/src/rslidar_sdk/config/config.yaml`：
```yaml
lidar_type: RS16        # 16线 RoboSense
use_lidar_clock: false  # 使用系统时钟
```

### faster-lio c16.yaml 修改记录

```yaml
# 修改前 → 修改后
lidar_type: 4 → 2           # C16(Leishen) → Velodyne兼容(RoboSense RS16)
time_sync_en: true → false  # 关闭外部时间同步
extrinsic_est_en: false → true  # 开启外参在线估计
scan_line: 16                # 保持不变（RS16=16线）
time_scale: 1e3              # 保持不变（毫秒）
```

**注意：** 源码和 install 目录下的 c16.yaml 都要改，`start_slam.sh` 从 install 加载配置。

### 新问题：点云呈同心圆

启动 faster-lio 后 rviz 显示 **16 条同心圆环**（对应 RS16 的 16 条扫描线），不是房间轮廓。

**已排除的原因：**
- ✅ 时间戳同步正常（差 81ms）
- ✅ lidar_type 已改为 2
- ✅ time_sync_en 已改为 false
- ✅ extrinsic_est_en 已改为 true

**待查方向：**
- faster-lio 源码中 `lidar_type=2` 对 PointCloud2 的字段解析逻辑
- RoboSense RS16 的 PointCloud2 字段格式（xyz? xyzirt?）是否匹配
- install 目录下的配置是否确实同步了改动
- 坐标系 frame_id 是否匹配（IMU vs LiDAR）

---

## 八、当前状态（2026-06-17）

- **建图**：数据链路已通（IMU 200Hz + LiDAR 10Hz），faster-lio 能启动但点云呈同心圆，需继续调试配置
- **巡逻避障**：代码就绪，待导航栈配通
- **火情检测**：YOLO模型训练过，真机未跑
- **中央大脑**：框架完整，技能系统上线
- **Web面板**：完整，含地图选点
- **通信桥接**：Hermes ↔ Claude VM 文件桥接运行中
- **Hermes Skills**：已迁移至 E:/hermes/skills/
